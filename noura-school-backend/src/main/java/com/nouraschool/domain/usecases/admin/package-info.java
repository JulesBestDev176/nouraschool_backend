/**
 * Use cases administrateur.
 * Les opérations admin (CRUD) sont modélisées comme use cases dans ce package
 * ({@code Admin*UseCase} / {@code Admin*UseCaseImpl}) et exposées via les ressources REST sous {@code /api/admin/*}.
 */
package com.nouraschool.domain.usecases.admin;
